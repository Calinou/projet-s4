{% extends 'base.html.twig' %}

{% block title %}
    {% trans %}home.create_game{% endtrans %}
{% endblock %}

{% block body %}
    <div class="container">
        <h1>{% trans %}home.create_game{% endtrans %}</h1>
        <ul>
            {% for user in users %}
                <li><a href="{{ path('create_game', {'opponent': user.id}) }}">{{ user.username }}</a></li>
            {% endfor %}
        </ul>
    </div>
{% endblock %}
