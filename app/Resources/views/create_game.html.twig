{% extends 'base.html.twig' %}

{% block title %}
    {% trans %}home.createGame{% endtrans %}
{% endblock %}

{% block body %}
    <div class="container">
        <h1>{% trans %}home.createGame{% endtrans %}</h1>
        <ul>
            {% for user in users %}
                <li><a href="{{ path('create_game', {'opponent': user.id}) }}">{{ user.username }}</a></li>
            {% endfor %}
        </ul>
    </div>
{% endblock %}
